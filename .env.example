# Copy to .env and fill in. .env is gitignored — never commit it.
# Service account key is stored in macOS Keychain, not here.
# Store it once with:
#   security add-generic-password -a "bio-startup-space" -s "google-sa-datasets-498814" -w "$(cat ~/Downloads/your-key.json)"

GOOGLE_SHEET_ID=1rsmT-fYVXvPp0J4guOxrK3NScZ2ClLiPr-jXyErvri4
